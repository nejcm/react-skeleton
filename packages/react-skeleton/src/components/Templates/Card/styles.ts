import styled from 'styled-components';

export const Wrapper = styled.div`
  border: solid 1px rgba(0, 0, 0, 0.08);
  border-radius: 3px;
  padding: 1.25rem;

  .s-image {
    margin-bottom: 1rem;
  }
`;
