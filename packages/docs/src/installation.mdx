---
name: Installation
route: /installation
---

import {Hint} from '@nejcm/docz-theme-extended';

# Installation

React skeleton supports styled-components and emotion.

## Peer dependencies

React skeleton requires the following peer dependencies to work. Make sure to
have them installed in your project.

<Hint type="info">
  You can <strong>skip this step</strong> if those packages are already
  installed in your application.
</Hint>

```bash
# for styled-components
$ npm install react react-dom styled-components @babel/runtime

# for emotion
$ npm install react react-dom @emotion/styled @babel/runtime
```

## Install

Install the latest version of React skeleton by running the following command in
your project's root directory:

```bash
# for styled-components
$ npm install @nejcm/react-skeleton --save

# for emotion
$ npm install @nejcm/react-skeleton-emotion --save
```

## Use

```jsx
import React from 'react';
// for styled-components
import Skeleton, {Card, Comment} from '@nejcm/react-skeleton';
// for emotion
import Skeleton, {Card, Comment} from '@nejcm/react-skeleton-emotion';

const YourComponent = () => {
  return (
    <Skeleton>
      <Skeleton.Rectangle height={20} />
      <Skeleton.Circle width={40} height={40} />
    </Skeleton>
  );
};

export default YourComponent;
```
